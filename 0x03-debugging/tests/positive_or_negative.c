#include <stdlib.h>
#include <time.h>
#include "holberton.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void positive_or_negative(int i)
{

i = 0;
if (i == 0)
{
printf("%d is zero\n", i);
}
}